import React, { useState, useEffect } from 'react';


const AgeCalculatorHooks = (initialValue = 0) => {
  const [calculator, setCalculator] = useState(initialValue)

  const timesThree = () => {
    setCalculator()
  }
  
  return (
    <div>

    </div>
  ) 
};

// export default AgeCalculatorHooks;
